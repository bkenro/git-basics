# 総合課題：ポートフォリオサイトをチーム開発する

## この課題の位置づけ

本講座の最終演習です。講座で学んだ Git の知識と操作を、実務で起こる一連の流れとして通しで実行します。
この課題を自力で完走できることが、本講座の修了要件です。

## シナリオ

あなたはエンジニア志望者として、自分のプロフィールを掲載する静的な Web サイトを作成します。
このサイトは同僚のエンジニア「鈴木さん」と 2 人で開発する想定です。

学習環境では、**あなた自身が 2 つの作業ディレクトリを使い分けて一人二役を演じます**。

| 役 | 作業ディレクトリ | 設定する名前 |
|---|---|---|
| あなた（開発者 A） | `~/portfolio` | 自分の名前 |
| 鈴木さん（開発者 B） | `~/portfolio-suzuki` | `Hanako Suzuki` |

両者は GitHub 上の同一リポジトリを共有します。B の作業ディレクトリは、A が GitHub に公開したリポジトリを clone して作ります。

## 前提環境

- macOS / Linux / Windows（Windows は Git Bash または WSL）
- テキストエディタ（VS Code を推奨）
- GitHub のアカウント（無料アカウントで可）
- ターミナルから `git` コマンドが実行できること（コマンドの導入自体も課題フェーズ 0 に含みます）

## 題材とするファイル

`~/portfolio` に、最終的に以下の構成のサイトを作ります。

```
portfolio/
├── .gitignore
├── README.md
├── index.html
├── css/
│   └── style.css
└── images/
    └── profile.png
```

---

## フェーズ 0：Git を使える状態にする

1. `git` コマンドが実行できることと、そのバージョンを確認しなさい。
2. 自分がコミットしたときに履歴へ記録される名前とメールアドレスを、この端末全体の設定として登録しなさい。
3. 新しいリポジトリを作ったときの最初のブランチ名が `main` になるように設定しなさい。
4. コミットメッセージの入力に使うエディタを、自分が使うエディタに設定しなさい。
5. 改行コードの自動変換について、自分の OS に適した設定を行いなさい。
6. 現在の設定内容と、その設定がどのファイルに書かれているかを一覧で確認しなさい。
7. `git config` にどのようなオプションがあるかを、**Web 検索ではなく Git のヘルプ機能で**調べなさい。あわせて `--global` を外した場合の挙動がヘルプのどこに書かれているかを示しなさい。

**確認事項**：`--global` を付けた設定と付けない設定の違いを説明できること。

---

## フェーズ 1：リポジトリを作り、最初のコミットを行う

1. `~/portfolio` ディレクトリを作り、Git のリポジトリとして初期化しなさい。
2. `README.md`・`index.html`・`css/style.css` の 3 ファイルを作成しなさい。内容は下記「初期ファイルの内容」に従うこと。
3. この時点でリポジトリがこの 3 ファイルをどう認識しているかを確認しなさい。
4. 3 ファイルすべてをコミットしなさい。コミットメッセージは、何をしたかが第三者に伝わる日本語 1 行とすること。
5. コミットが 1 件記録されたことを、履歴の一覧で確認しなさい。

**確認事項**：`git add` の前後で `git status` の表示がどう変わったか、それが何を意味するかを説明できること。

---

## フェーズ 2：無視するファイルを設定し、コミットを意味のある単位に分ける

1. 作業中に自動生成される以下のファイル・ディレクトリを、Git の追跡対象から除外する設定を行いなさい。
   - OS が生成するファイル（`.DS_Store`、`Thumbs.db`）
   - 拡張子が `.log` のファイル
   - `tmp/` ディレクトリ以下すべて
2. 除外設定そのものは履歴に残さなければなりません。除外設定のファイルだけをコミットしなさい。
3. 動作確認のため `tmp/work.log` を作成し、除外が効いていることを確認しなさい。
4. 次に、以下の **2 種類の変更を同時に**行いなさい。
   - `images/profile.png` を追加し、`index.html` からその画像を表示する
   - `README.md` に、このサイトの目的を 1 段落追記する
5. この 2 種類の変更を、**1 つのコミットにまとめず、2 つのコミットに分けて**記録しなさい。
6. 次の小実験を行いなさい。
   - `README.md` を `Readme.md` にリネームし、`git status` の出力を確認する
   - 出力が「変更なし」だった場合と「削除＋未追跡ファイル」だった場合で、自分の環境に何の違いがあるかを説明する
   - 実験後は `README.md` に戻し、リポジトリを clean な状態にする

**確認事項**：
- フェーズ 2 の 5 で、なぜコミットを分けるほうがよいのかを説明できること。
- フェーズ 2 の 6 の結果が OS によって異なる理由と、それが引き起こしうる不具合を説明できること。

---

## フェーズ 3：GitHub に公開する

1. GitHub 上に `portfolio` という名前のリポジトリを作成しなさい。README や .gitignore の自動生成は行わないこと。
   - Public と Private の**どちらを選んだか、およびその理由**を述べなさい。
   - あわせて、このリポジトリに置いてはいけない種類のファイルを 3 つ挙げなさい。
2. SSH 鍵を生成し、公開鍵を GitHub アカウントに登録しなさい。
3. GitHub への SSH 接続が成立することを確認しなさい。
4. ローカルリポジトリに、GitHub 上のリポジトリを `origin` という名前のリモートとして登録しなさい。
5. `main` ブランチを GitHub へ送信しなさい。その際、以後は追加のオプションなしで送受信できるように設定すること。
6. GitHub の Web 画面で、コミット履歴が反映されていることを確認しなさい。
7. GitHub のリポジトリのトップページに `README.md` の内容が表示されていることを確認しなさい。
8. 初めてこのリポジトリを訪れた人が内容を把握できるよう、`README.md` に「このサイトの目的」「ローカルでの表示方法」の 2 項目を整えてコミットし、送信しなさい。

**確認事項**：
- フェーズ 3 の 5 で設定した「追跡ブランチ」とは何を指すか説明できること。
- README に書くべきこと／書くべきでないことを説明できること。

---

## フェーズ 4：ブランチを切って機能を追加する

1. `feature/works-section` という名前のブランチを作成し、そのブランチへ移動しなさい。
2. このブランチ上で、以下の 3 つの作業を行いなさい。
   - `index.html` に「制作物」セクション（`<section id="works">`）を追加する
   - `css/style.css` に制作物セクション用のスタイルを追加する。その際、色は `:root` に定義した CSS 変数 `--accent-color` を新設して参照すること
   - `README.md` にサイトの構成（ディレクトリ構造）の説明を追記する
3. この 3 つの作業を、**3 つのコミットに分けて**記録しなさい。
4. このブランチを GitHub へ送信しなさい。
5. `main` と `feature/works-section` の分岐の様子を、グラフ表示で確認しなさい。

**確認事項**：ブランチを切り替えたとき、作業ディレクトリのファイルに何が起きているかを説明できること。

---

## フェーズ 5：もう一人の開発者として main を更新する

ここからは鈴木さん（開発者 B）の役です。

1. `~/portfolio-suzuki` に、GitHub 上のリポジトリを clone しなさい。ただし、**HTTPS 形式の URL**（`https://github.com/...`）を使うこと。
2. **このリポジトリだけに**適用される設定として、名前を `Hanako Suzuki`、メールアドレスを `hanako@example.com` に設定しなさい。
3. `css/style.css` の `:root` にある `--main-color` の値を、落ち着いた色（例：`#334155`）に変更しなさい。
4. 変更をコミットし、`main` ブランチとして GitHub へ送信しなさい。
5. 送信時に認証を求められ、GitHub のパスワードでは通らないはずです。原因を説明したうえで、**このリポジトリのリモート URL を SSH 形式に変更して**送信を完了させなさい。

**確認事項**：
- フェーズ 5 の 2 で `--global` を使ってはいけない理由を説明できること。
- HTTPS 形式と SSH 形式の URL の違い、および clone は成功したのに push で失敗した理由を説明できること。

---

## フェーズ 6：他人の変更を取り込み、コンフリクトを解消する

開発者 A（`~/portfolio`）の役に戻ります。

1. GitHub 側の最新状態を取得しなさい。ただし、**この時点では自分の作業ディレクトリのファイルを書き換えないこと**。
2. 自分のローカルの `main` と GitHub 側の `main` に、どのような差があるかを、次の 2 つの観点で確認しなさい。
   - **どのコミットが増えるのか**（コミットの一覧として）
   - **どのファイルがどう変わるのか**（差分として）
3. ローカルの `main` を GitHub 側の最新状態に合わせなさい。
4. `feature/works-section` ブランチに、`main` の変更を取り込みなさい。
5. `css/style.css` でコンフリクトが発生します。以下を行いなさい。
   - どのファイルがコンフリクトしているかをコマンドで確認する
   - ファイル中に現れる `<<<<<<<`・`=======`・`>>>>>>>` の各部分が、それぞれ誰の変更かを説明する
   - **両者の変更を活かす形で**解消する（鈴木さんの `--main-color` の変更と、自分が追加した `--accent-color` の両方が残ること）
6. 解消結果を記録し、GitHub へ送信しなさい。
7. 履歴のグラフ表示で、2 本の流れが 1 本に合流したことを確認しなさい。
8. 次の状況を意図的に作り、`git pull` の挙動を確認しなさい。
   - `~/portfolio-suzuki`（鈴木さん役）で `docs/CONTRIBUTING.md` を新規作成してコミットし、`main` に push する
   - `~/portfolio`（自分）で `main` に切り替え、**pull せずに** `docs/memo.md` を新規作成してコミットする
   - その状態で `git pull` を実行し、表示されるメッセージを読み取る
   - メッセージの指示に従い、このリポジトリでの `pull` の既定動作を設定したうえで取り込みを完了させる

**確認事項**：
- `git fetch` と `git pull` の違いを説明できること。
- フェーズ 6 の 8 で警告が出た理由を、ローカルとリモートの履歴の形から説明できること。
- 今回のコンフリクトを**発生させずに済ませる**には、A と鈴木さんがどう作業を進めればよかったかを 3 つ挙げること。

---

## フェーズ 7：Pull Request でレビューを受けてマージする

1. GitHub 上で、`feature/works-section` から `main` への Pull Request を作成しなさい。タイトルと説明は、レビュアーが変更内容を把握できる内容とすること。
2. 鈴木さんからの以下のレビュー指摘を受けた、という想定で対応しなさい。
   > 追加された `<img>` に `alt` 属性がありません。修正をお願いします。
3. `feature/works-section` ブランチ上で指摘を修正し、コミットして送信しなさい。修正が Pull Request に自動で反映されることを確認しなさい。
4. Pull Request を **squash マージ**でマージしなさい。
5. ローカルに戻り、`main` を最新状態に更新しなさい。
6. 不要になった `feature/works-section` ブランチを、ローカルと GitHub の両方から削除しなさい。

**確認事項**：フェーズ 7 の 6 でローカルブランチを削除しようとすると警告が出ます。その原因を、squash マージの仕組みから説明できること。

---

## フェーズ 8：事故に対処する

以下の 3 つの事故を意図的に発生させ、それぞれ適切な方法で復旧しなさい。
**各ケースで、なぜその方法を選んだのかを説明できること**が最も重要です。

### ケース 1：機密ファイルを含むコミットを作ってしまった（まだ push していない）

1. `config/api-key.txt` を作成し、中に `SECRET_TOKEN=abcdef123456` と書きなさい。
2. `index.html` の見出しを修正しなさい。
3. この 2 つを、うっかり `git add .` でまとめてコミットしなさい。
4. コミット直後に、機密ファイルが混入していることに気づきました。**`index.html` の修正内容は失わずに**、機密ファイルだけをコミットから取り除きなさい。
5. 今後同じ事故が起きないよう、`config/` を追跡対象から除外しなさい。
6. `index.html` の修正を、改めて単独でコミットしなさい。

### ケース 2：誤った内容をコミットし、push もしてしまった

1. `README.md` に誤った説明を追記してコミットし、GitHub へ送信しなさい。
2. 送信後に誤りに気づきました。**履歴を書き換えずに**、この変更を打ち消しなさい。
3. 打ち消した結果を GitHub へ送信しなさい。

### ケース 3：コミットを消してしまった

1. 何か 1 つ変更を加えてコミットしなさい。
2. 誤操作の想定として、`git reset --hard HEAD~1` を実行し、そのコミットを履歴から消しなさい。
3. 消したはずのコミットを見つけ出し、復旧しなさい。

**確認事項**：ケース 1・2・3 で使った 3 つの手法の使い分けを、「push 済みかどうか」という観点で整理して説明できること。
また、ケース 2 の方法では GitHub 上の履歴から機密情報そのものは消えません。もし機密情報を push してしまった場合に**最初に取るべき行動**を答えなさい。

---

## フェーズ 9：リリースと履歴の説明

1. `main` ブランチが最新であることを確認しなさい。
2. 現在の状態に `v1.0.0` というタグを、メッセージ付きで付与しなさい。
3. タグを GitHub へ送信し、Web 画面で確認しなさい。
4. 履歴全体をグラフ形式・1 行表示で出力しなさい。
5. その出力を見ながら、**このリポジトリで何がどの順に起きたかを、Git を知らない人にも伝わるように 200 字程度で説明しなさい**。

---

## 提出物

1. `~/portfolio` の GitHub リポジトリ URL
2. `git log --oneline --graph --decorate --all` の実行結果
3. フェーズ 8 の 3 ケースの使い分けに関する説明（自由記述）
4. フェーズ 9 の 5 の履歴の説明文

## 評価チェックリスト

| # | 観点 | 対応フェーズ |
|---|---|---|
| 1 | Git を使うための環境と設定を自分で準備できる | 0, 3 |
| 2 | 作業ツリー・インデックス・リポジトリの 3 領域を理解している | 1, 2 |
| 3 | コミットを意味のある単位に分けられる | 2, 4 |
| 4 | ブランチの生成・切り替え・統合ができる | 4, 6, 7 |
| 5 | コンフリクトの内容を読み取り、適切に解消できる | 6 |
| 6 | リモートリポジトリとの同期操作を理解して使える | 3, 5, 6 |
| 7 | Pull Request によるチーム開発の流れを実行できる | 7 |
| 8 | 事故の種類に応じた復旧方法を選択できる | 8 |
| 9 | 履歴を読み、他者に説明できる | 4, 6, 9 |
| 10 | 環境差異による落とし穴を認識している | 0, 2, 5 |
| 11 | 公開範囲と機密情報の扱いを判断できる | 3, 8 |
| 12 | エラーメッセージとヘルプから自力で解決できる | 0, 5, 6 |

---

## 初期ファイルの内容

### `README.md`（フェーズ 1 時点）

```markdown
# ポートフォリオサイト

自己紹介と制作物を掲載する静的な Web サイト。
```

### `index.html`（フェーズ 1 時点）

```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>山田太郎のポートフォリオ</title>
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <header>
    <h1>山田太郎</h1>
    <p>エンジニアを目指して学習中です。</p>
  </header>
</body>
</html>
```

### `css/style.css`（フェーズ 1 時点）

```css
:root {
  --main-color: #1e88e5;
  --bg-color: #ffffff;
}

body {
  font-family: sans-serif;
  color: var(--main-color);
  background: var(--bg-color);
  margin: 0 auto;
  max-width: 720px;
  padding: 24px;
}
```
